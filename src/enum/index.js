const USERSTATUS = require('./user-status');
const REGION = require('./region');
const GRANTTYPE = require('./grant-type');
const SPAPIURI = require('./sp-api-uri');

module.exports = {
    USERSTATUS,
    REGION,
    GRANTTYPE,
    SPAPIURI
}